# 画面遷移図 — デルカク✓

画面IDは [02-screens.md](02-screens.md) と対応。

```mermaid
flowchart TD
    Start([起動]) -->|初回のみ| S01[S01 オンボーディング]
    Start -->|2回目以降| S02
    S01 -->|通知許可後| S02[S02 ホーム]

    通知[通知タップ] --> S03

    S02 -->|今すぐチェック / 予定をタップ| S03[S03 チェック実行]
    S03 -->|全項目チェック完了| S04[S04 完了]
    S03 -->|あとで| S05[S05 スヌーズ選択]
    S05 -->|時間選択| S02
    S04 -->|ホームへ| S02

    S02 -->|項目管理| S06[S06 項目一覧]
    S06 -->|追加 / 編集| S07[S07 項目追加・編集]
    S07 -->|保存 / 削除| S06

    S02 -->|セット管理| S08[S08 セット一覧]
    S08 -->|追加 / 編集| S09[S09 セット追加・編集]
    S09 -->|保存 / 削除| S08
    S09 -.->|項目が未登録なら| S07

    S02 -->|カレンダー| S10[S10 カレンダー]
    S10 -->|日付をタップ| S11[S11 予定追加・編集]
    S11 -->|保存 / 削除| S10
    S11 -.->|セット未登録なら| S08

    S02 -->|設定| S12[S12 設定]
    S12 -->|ホームへ戻る| S02

    S03 -.->|重複統合プレビュー任意| S13[S13 重複統合確認]
    S13 -.-> S03
```

## 主要な遷移ルール

1. **初回起動のみ S01 を経由する。** 2回目以降はホーム（S02）が常に起点。
2. **通知タップは常に S03（チェック実行）へ直行する。** ホームを経由させない（3タップ以内の方針に合致）。
3. **S03 → S04 は自動遷移。** 全項目チェックでユーザー操作なしに完了画面へ進む。
4. **S04 からの遷移先はホームのみ。** 完了画面で迷わせないため選択肢を絞る。
5. **項目／セット未登録時の誘導。** セット作成画面（S09）で項目が0件の場合、項目追加（S07）へ誘導。予定作成画面（S11）でセットが0件の場合、セット一覧（S08）へ誘導する。これにより「使い始めの詰まり」を防ぐ。
6. **S13（重複統合確認）は任意機能。** MVPでは実装せず、将来必要に応じて S03 の直前に挿入する設計とする（点線で表現）。
